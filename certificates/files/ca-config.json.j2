{
    "signing": {
        "default": {
            "expiry": "{{ ca.config.expiry }}"
        },
        "profiles": {
            "server": {
                "expiry": "{{ ca.config.expiry }}",
                "usages": [
                    "signing",
                    "key encipherment",
                    "server auth",
                    "client auth"
                ]
            },
            "client": {
                "expiry": "{{ ca.config.expiry }}",
                "usages": [
                    "signing",
                    "key encipherment",
                    "client auth"
                ]
            }
        }
    }
}
